name: Extra Landing page
type: Drupal CMS
description: 'Provides a minimal content type (mt-landing-page) optimized for Layout Builder, allowing flexible block-based landing page creation.'

config:
  strict: false
