# Database connection for local (non-DDEV) installation
DB_HOST=localhost
DB_PORT=3306
DB_NAME=db
DB_USER=user
DB_PASS=password

# Derived DB URL (used by Drush)
DB_URL=mysql://user:password@localhost:3306/db